services:
  api:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - model-data:/app/models
    restart: unless-stopped

  web:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  model-data:
